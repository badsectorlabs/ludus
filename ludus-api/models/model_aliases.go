package models

// These are aliases so that the models make more sense in code when used instead of always being plural

// Alias for the Users model
type User = Users

// Alias for the Groups model
type Group = Groups

// Alias for the Ranges model
type Range = Ranges

// Alias for the Templates model
type Template = Templates

// Alias for the VMs model
type VM = VMs

// Alias for the Logs model
type Log = Logs
